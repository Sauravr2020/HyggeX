function Footer() {
  return (
    <footer className="w-70vw ml-10p mr-20 mt-2 ml-20">
      <div>
        <span className="text-xl text-indigo-900 font-bold">FAQ</span>
      </div>
      <div className="border border-indigo-900 rounded-md p-1 mt-2 ml-2 mb-2 flex justify-between items-center">
        <span>Can education flashcard be used for all age groups?</span>
        <select name="city"></select>
      </div>
      <div className="border border-indigo-900 rounded-md p-1 mt-2 ml-2 mb-2 flex justify-between items-center">
        <span>How do education flashcards work?</span>
        <select name="city"></select>
      </div>
      <div className="border border-indigo-900 rounded-md p-1 mt-2 ml-2 mb-2 flex justify-between items-center">
        <span>Can education flashcard be used test preparation?</span>
        <select name="city"></select>
      </div>
    </footer>
  );
}

export default Footer;

  );
}

export default Footer;
